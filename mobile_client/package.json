{
  "name": "android_client",
  "version": "1.0.0",
  "description": "ClaudeD Android Client",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build-web && npx cap sync android",
    "build-web": "echo 'No build step needed for vanilla JS'",
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build-apk": "cd android && ./gradlew assembleDebug",
    "build-aab": "cd android && ./gradlew assembleRelease"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.0"
  }
}
